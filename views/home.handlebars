<h1> HOME - Productos existentes </h1>
{{#each products}}
<div>
    <h2>{{this.title}}</h2>
    <ul>{{this.title}}</ul>
    <ul>{{this.description}}</ul>
    <ul>{{this.price}}</ul>
    <ul>{{this.category}}</ul>
    </br>
</div>
{{/each}}
